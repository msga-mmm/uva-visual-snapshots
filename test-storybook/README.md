# Test Storybook

Simple Storybook fixture project for testing `uva-visual-snapshots` capture and compare flows.

## Run

```bash
cd test-storybook
npm install
npm run storybook
```

Storybook starts at `http://localhost:6006`.

## Build

```bash
npm run build-storybook
```
